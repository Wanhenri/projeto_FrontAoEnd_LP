import { createGlobalStyle} from "styled-components";

const Size = createGlobalStyle `
    :root {
        --radius-small: 3px;
        --size-base:10px;
        --size-small:14px;
        --size-medium:18px;
        --size-very-big:5em;
    }

`

export default Size;

